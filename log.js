/* PREVIEW BUILD — analytics disabled. Replace with source/log.js to re-enable. */
function logEvent(name, payload) { console.log('[preview: analytics disabled]', name, payload); }
window.logEvent = logEvent;
